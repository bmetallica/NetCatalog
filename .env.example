DB_HOST=localhost
DB_PORT=5432
DB_NAME=netcatalog
DB_USER=netcatalog
DB_PASSWORD=changeme

# NOTE: Scan settings are configured via Web UI (Settings page)
# Not in .env - they're stored in database and can be changed anytime
PORT=3000
